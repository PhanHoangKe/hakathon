@using System.Collections.Generic
@using hakathon.Models;
@model List<tblRoles>

@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<main id="main" class="main">
    <div class="pagetitle">
        <h2>Danh Sách Vai Trò</h2>
    </div>

    <section class="section dashboard">
        <div class="row">
            <div class="col-12">
                <div class="card recent-sales overflow-auto">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <a type="button" class="btn btn-success" asp-area="Admin" asp-controller="Roles" asp-action="Create">
                            <i class="bi bi-plus-circle me-1"></i>Thêm Vai Trò
                        </a>
                    </div>

                    <div class="card-body mt-4">
                        <table class="table table-hover table-bordered datatable" style="background-color: white;">
                            <thead class="thead-light">
                                <tr>
                                    <th class="text-center">ID Vai Trò</th>
                                    <th>Tên Vai Trò</th>
                                    <th class="text-center">Chức Năng</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td class="text-center" scope="row">@item.RoleID</td>
                                        <td>@item.RoleName</td>
                                        <td class="text-center">
                                            <a asp-area="Admin" asp-controller="Roles" asp-action="Edit"
                                               asp-route-id="@item.RoleID" class="btn btn-primary btn-sm"
                                               title="Chỉnh sửa vai trò"><i class="bi bi-pencil"></i></a>
                                            <a asp-area="Admin" asp-controller="Roles" asp-action="Delete"
                                               asp-route-id="@item.RoleID" class="btn btn-danger btn-sm"
                                               title="Xóa vai trò" onclick="return confirm('Bạn có chắc chắn muốn xóa vai trò này không?');"><i class="bi bi-trash"></i></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>