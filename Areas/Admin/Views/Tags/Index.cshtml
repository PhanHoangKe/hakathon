@using System.Collections.Generic
@using hakathon.Models;
@model List<tblTags>

@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<main id="main" class="main">
    <div class="pagetitle">
        <h2>Danh Sách Thẻ</h2>
    </div>

    <section class="section dashboard">
        <div class="row">
            <div class="col-12">
                <div class="card recent-sales overflow-auto">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <a type="button" class="btn btn-success" asp-area="Admin" asp-controller="Tags" asp-action="Create">
                            <i class="bi bi-plus-circle me-1"></i>Thêm Thẻ
                        </a>
                    </div>

                    <div class="card-body mt-4">
                        <table class="table table-hover table-bordered datatable" style="background-color: white;">
                            <thead class="thead-light">
                                <tr>
                                    <th class="text-center">ID Thẻ</th>
                                    <th>Tên Thẻ</th>
                                    <th class="text-center">Ngày Tạo</th>
                                    <th class="text-center">Hoạt Động</th>
                                    <th class="text-center">Chức Năng</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td class="text-center" scope="row">@item.TagID</td>
                                        <td>@item.TagName</td>
                                        <td class="text-center">@item.CreatedDate?.ToString("dd/MM/yyyy HH:mm")</td>
                                        <td class="text-center">@item.IsActive</td>
                                        <td class="text-center">
                                            <a asp-area="Admin" asp-controller="Tags" asp-action="Edit"
                                               asp-route-id="@item.TagID" class="btn btn-primary btn-sm"
                                               title="Chỉnh sửa thẻ"><i class="bi bi-pencil"></i></a>
                                            <a asp-area="Admin" asp-controller="Tags" asp-action="Delete"
                                               asp-route-id="@item.TagID" class="btn btn-danger btn-sm"
                                               title="Xóa thẻ" onclick="return confirm('Bạn có chắc chắn muốn xóa thẻ này không?');"><i class="bi bi-trash"></i></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
</main>